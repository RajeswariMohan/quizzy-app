# Dev (recommended): Vite proxies /api → http://localhost:3000
VITE_API_BASE_URL=/api

# Or call the API directly (backend CORS must allow localhost:5173):
# VITE_API_BASE_URL=http://localhost:3000/api
